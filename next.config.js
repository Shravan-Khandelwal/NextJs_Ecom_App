// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["img.daisyui.com", "images.unsplash.com","cdn.dummyjson.com"], // Add the domain here
  },
};

module.exports = nextConfig;
